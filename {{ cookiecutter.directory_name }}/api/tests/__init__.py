"""Unit and integration tests

All files named like `test_something.py` will be executed when running tests.

`conftest.py` defines several fixtures that are useful for testing (namely a
fixture for obtaining a test-specific database session).
"""
